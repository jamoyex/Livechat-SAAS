<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Your Business</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: { accent: '#C7FF00', card: '#F5F7F9', muted: '#6B7280', text: '#000000', background: '#FFFFFF' },
          borderRadius: { lg: '24px' }
        }
      }
    }
  </script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@shadcn/ui@latest/dist/style.css">
  <link href="/css/style.css" rel="stylesheet">
  <style>
    html, body { height: 100%; min-height: 100vh; }
    body {
      min-height: 100vh;
      background: var(--background-color);
      font-family: 'Inter', system-ui, sans-serif;
      display: flex;
      flex-direction: column;
      margin: 0;
    }
    .add-business-header {
      width: 100%;
      padding: 32px 0 24px 0;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .logo-img {
      height: 70px;
      width: auto;
    }
    .add-business-container {
      flex: 1 1 auto;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 0;
      padding-bottom: 48px;
    }
    .add-business-card {
      background: #fff;
      border-radius: 18px;
      box-shadow: 0 4px 32px rgba(59,130,246,0.10), 0 1.5px 4px rgba(0,0,0,0.04);
      padding: 2.5rem 2.5rem 2rem 2.5rem;
      max-width: 420px;
      width: 100%;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .add-business-card h2 {
      font-weight: 700;
      font-size: 2rem;
      margin-bottom: 1.5rem;
      color: #2563eb;
      letter-spacing: 0.5px;
      text-align: center;
    }
    form {
      width: 100%;
      display: flex;
      flex-direction: column;
      gap: 1.2rem;
    }
    label {
      font-weight: 500;
      color: #222;
      margin-bottom: 0.4rem;
    }
    input[type="text"] {
      border-radius: 8px;
      border: 1px solid #d1d5db;
      padding: 12px 16px;
      font-size: 1rem;
      background: #f8f9fa;
      transition: border 0.18s, box-shadow 0.18s;
    }
    input[type="text"]:focus {
      border-color: var(--accent-color);
      box-shadow: 0 0 0 2px var(--accent-color);
      outline: none;
    }
    button[type="submit"] {
      background: var(--accent-color);
      color: var(--text-color);
      font-weight: 600;
      border: none;
      border-radius: var(--radius-lg);
      padding: 12px 0;
      font-size: 1.1rem;
      margin-top: 0.5rem;
      transition: opacity var(--transition-fast), transform var(--transition-fast);
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    }
    button[type="submit"]:hover {
      opacity: 0.85;
      transform: scale(0.98);
    }
    @media (max-width: 600px) {
      .add-business-card {
        padding: 1.5rem 1rem 1rem 1rem;
        max-width: 98vw;
      }
      .add-business-header {
        padding: 20px 0 12px 0;
      }
    }
    .form-group-centered {
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
      margin-bottom: 1.2rem;
    }
    .form-group-centered label {
      font-weight: 600;
      font-size: 1.1rem;
      color: #222;
      margin-bottom: 0.7rem;
      text-align: center;
    }
    .form-group-centered input[type="text"] {
      width: 100%;
      max-width: 320px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="add-business-header">
    <img src="/images/BotBuilders_Logo_White_2.png" alt="BotBuilders Logo" class="logo-img">
  </div>
  <div class="add-business-container">
    <div class="add-business-card">
      <h2>Create Your Business</h2>
      <form action="/business/add" method="POST">
        <div class="form-group-centered">
          <label for="businessName">Business Name</label>
          <input type="text" id="businessName" name="name" required>
        </div>
        <!-- Widget settings defaults -->
        <input type="hidden" name="widget_header_color" value="#007bff">
        <input type="hidden" name="widget_h1_color" value="#ffffff">
        <input type="hidden" name="widget_button_color" value="#007bff">
        <input type="hidden" name="widget_visitor_message_color" value="#007bff">
        <input type="hidden" name="widget_quick_replies" value="">
        <button type="submit">Create Business</button>
      </form>
    </div>
  </div>
</body>
</html> 