<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Botbuilders Chat Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        html, body { height: 100%; min-height: 100vh; }
        body { min-height: 100vh; background: linear-gradient(135deg, #f6f8fa 0%, #e0e7ff 100%); height: 100vh; overflow: hidden; }
        .dashboard-header {
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
            background: linear-gradient(90deg, #2563eb 0%, #22c55e 100%);
            border-bottom: none;
        }
        .dashboard-header .logo-img {
            height: 60px;
            width: auto;
        }
        .dashboard-root { display: flex; height: calc(100vh - 72px); }
        .sidebar {
            border-right: none;
            height: 100%;
            background: #fff;
            box-shadow: 2px 0 16px rgba(59,130,246,0.08);
            display: flex;
            flex-direction: column;
            min-width: 260px;
            max-width: 350px;
            padding-top: 12px;
        }
        .sidebar-nav { margin-top: 2rem; }
        .sidebar-nav .nav-link {
            color: #2563eb;
            border-radius: 10px;
            font-weight: 500;
            margin-bottom: 8px;
            padding: 12px 18px;
            font-size: 1.08rem;
            display: flex;
            align-items: center;
            gap: 12px;
            transition: background 0.18s, color 0.18s;
        }
        .sidebar-nav .nav-link i { font-size: 1.2rem; }
        .sidebar-nav .nav-link.active {
            background: linear-gradient(90deg, #2563eb 0%, #22c55e 100%);
            color: #fff;
            font-weight: 700;
            box-shadow: 0 2px 8px rgba(59,130,246,0.08);
        }
        .sidebar-nav .nav-link:hover:not(.active) {
            background: #f1f5f9;
            color: #1e293b;
        }
        .sidebar-nav .nav-link.text-danger {
            color: #ef4444 !important;
        }
        .sidebar-nav .nav-link.text-danger:hover {
            background: #fee2e2;
            color: #b91c1c !important;
        }
        .main-content { height: 100%; overflow: hidden; display: flex; flex-direction: column; padding: 0; background: #fff; }
        .tab-section { display: none; height: 100%; }
        .tab-section.active { display: flex; flex-direction: column; height: 100%; }
        .tab-section.scrollable {
            overflow-y: auto;
            flex: 1 1 auto;
            height: 100%;
        }
        .chat-area {
            display: flex;
            flex-direction: column;
            background: #fff;
            height: 100%;
            border-radius: 0;
            margin-top: 0;
            box-shadow: none;
        }
        .chat-header {
            background: #f8fafc;
            border-bottom: 1px solid #e9ecef;
            padding: 1.25rem;
            border-radius: 0;
        }
        .chat-row { flex: 1 1 0; display: flex; min-height: 0; }
        .conversations-col {
            display: flex;
            flex-direction: column;
            height: 100%;
            border-right: 1px solid #e9ecef;
            flex: 0 0 40%;
            max-width: 40%;
            min-width: 0;
            background: #f6f8fa;
            border-radius: 0;
        }
        .conversations-header { flex: 0 0 auto; }
        #conversationsList { flex: 1 1 auto; overflow-y: auto; min-height: 0; }
        .load-more-container { flex: 0 0 auto; }
        .chat-messages-col {
            display: flex;
            flex-direction: column;
            height: 100%;
            flex: 0 0 60%;
            max-width: 60%;
            min-width: 0;
            background: #f8f9fa;
            border-radius: 0;
        }
        .chat-messages {
            flex: 1 1 auto;
            overflow-y: auto;
            padding: 2rem 2.5rem 1.5rem 2.5rem;
            display: flex;
            flex-direction: column;
            gap: 1.25rem;
            min-height: 0;
        }
        .message {
            max-width: 80%;
            padding: 16px 20px;
            border-radius: 18px;
            font-size: 15px;
            line-height: 1.6;
            position: relative;
            word-wrap: break-word;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        .message.user {
            align-self: flex-start;
            background: #f0f0f0;
            color: #1a1a1a;
            border-bottom-left-radius: 6px;
            margin-right: auto;
        }
        .message.bot {
            align-self: flex-end;
            background: #2563eb;
            color: #fff;
            border-bottom-right-radius: 6px;
            margin-left: auto;
        }
        .message.agent {
            align-self: flex-end;
            background: #22c55e;
            color: #fff;
            border-bottom-right-radius: 6px;
            margin-left: auto;
        }
        .message-avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: #e0e7ff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            font-weight: 700;
            color: #2563eb;
        }
        .message.bot .message-avatar { background: #2563eb; color: #fff; }
        .message.agent .message-avatar { background: #22c55e; color: #fff; }
        .message.user .message-avatar { background: #f0f0f0; color: #2563eb; }
        .chat-input-area {
            background: #fff;
            border-top: 1px solid #e9ecef;
            padding: 1.25rem;
            border-radius: 0;
            flex: 0 0 auto;
        }
        #messageForm {
            display: flex;
            gap: 12px;
            align-items: center;
        }
        #messageInput {
            flex-grow: 1;
            padding: 12px 18px;
            border: 1px solid #ced4da;
            border-radius: 24px;
            font-size: 1rem;
            background-color: #f8f9fa;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        #messageInput:focus {
            outline: none;
            border-color: #2563eb;
            box-shadow: 0 0 0 0.2rem #2563eb33;
        }
        .send-btn {
            color: #fff;
            background: #2563eb;
            border: none;
            border-radius: 50%;
            width: 48px;
            height: 45px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            transition: background 0.18s;
        }
        .send-btn:hover {
            background: #2563eb;
        }
        .status-badge {
            padding: 2px 10px;
            border-radius: 12px;
            font-size: 0.85rem;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            width: auto;
            text-align: center;
            line-height: 1.5;
        }
        .status-active { background: #ecfdf5; color: #059669; }
        .status-handled { background: #fff7ed; color: #ea580c; }
        .status-pending { background: #f0f9ff; color: #0284c7; }
        .status-badge i { font-size: 1rem; }
        .unread-preview { background: rgba(59, 130, 246, 0.05); }
        .unread-message { font-weight: 600; color: #1a1a1a; }
        .team-member-card { transition: all 0.2s; border-radius: 10px; }
        .team-member-card:hover { transform: translateY(-2px); box-shadow: 0 2px 8px rgba(59,130,246,0.08); }
        .widget-preview-container {
            border: 1px solid #e9ecef;
            border-radius: 12px;
            background: #f8f9fa;
            padding: 1.5rem;
            display: flex;
            justify-content: center;
        }
        .widget-preview-iframe {
            width: 100%;
            min-height: 600px;
            border: none;
            border-radius: 16px;
            background: #fff;
            max-width: 500px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.10), 0 1.5px 4px rgba(0,0,0,0.04);
        }
        h4, h5, h6 { font-weight: 700; color: #222; letter-spacing: 0.5px; }
        .card { border-radius: 12px; box-shadow: 0 2px 12px rgba(0,0,0,0.04); }
        .form-control, .form-select {
            border-radius: 8px;
            border: 1px solid #d1d5db;
            font-size: 1rem;
            transition: border 0.18s, box-shadow 0.18s;
        }
        .form-control:focus, .form-select:focus {
            border-color: #2563eb;
            box-shadow: 0 0 0 2px #2563eb22;
        }
        .btn-primary {
            background: #2563eb;
            border-color: #2563eb;
            font-weight: 600;
            border-radius: 8px;
            transition: background 0.18s, border 0.18s;
        }
        .btn-primary:hover {
            background: #1d4ed8;
            border-color: #1d4ed8;
        }
        .btn-outline-primary {
            color: #2563eb;
            border-color: #2563eb;
            border-radius: 8px;
        }
        .btn-outline-primary:hover {
            background: #2563eb;
            color: #fff;
        }
        .btn-outline-danger {
            border-radius: 8px;
        }
        .btn-outline-success {
            border-radius: 8px;
        }
        .alert-info {
            border-radius: 8px;
        }
        /* Responsive adjustments */
        @media (max-width: 991.98px) {
            .dashboard-root, .sidebar, .main-content, .tab-section.active { height: auto !important; min-height: 0 !important; }
        }
    </style>
</head>
<body>
    <!-- Dashboard Header -->
    <header class="dashboard-header d-flex align-items-center px-4 py-3 border-bottom justify-content-between" style="height: 72px;">
        <img src="/images/BotBuilders_Logo_White_2.png" alt="Logo" class="logo-img">
        <div class="form-check form-switch ms-2">
            <input class="form-check-input" type="checkbox" id="notificationToggle">
            <label class="form-check-label text-white" for="notificationToggle">Notification Sound</label>
        </div>
    </header>
    <div class="dashboard-root">
        <!-- Sidebar Navigation -->
        <div class="sidebar col-12 col-md-3 p-0 d-flex flex-column">
            <div class="p-3 border-bottom bg-white d-flex align-items-center justify-content-between">
                <div>
                    <h5 class="mb-0">Your Businesses</h5>
                    <select id="businessSelector" class="form-select mt-2">
                        <% businesses.forEach(function(biz) { %>
                            <option value="<%= biz.id %>" <%= biz.id === selectedBusiness.id ? 'selected' : '' %>><%= biz.name %></option>
                        <% }); %>
                    </select>
                </div>
            </div>
            <nav class="sidebar-nav nav flex-column nav-pills p-2 gap-2">
                <a class="nav-link active" id="tab-chats" href="#" data-tab="chats"><i class="fas fa-comments me-2"></i>Chats</a>
                <a class="nav-link" id="tab-team" href="#" data-tab="team"><i class="fas fa-users me-2"></i>Team Members</a>
                <a class="nav-link" id="tab-widget" href="#" data-tab="widget"><i class="fas fa-cog me-2"></i>Widget Settings</a>
                <a class="nav-link" id="tab-ai" href="#" data-tab="ai"><i class="fas fa-robot me-2"></i>AI Settings</a>
                <a class="nav-link" id="tab-training" href="#" data-tab="training"><i class="fas fa-graduation-cap me-2"></i>AI Training Info</a>
                <a class="nav-link" id="tab-install" href="#" data-tab="install"><i class="fas fa-code me-2"></i>Install Widget</a>
                <a class="nav-link mt-auto text-danger" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Logout</a>
            </nav>
        </div>
        <!-- Main Content Area -->
        <div class="main-content col-12 col-md-9 p-0">
            <!-- Chats Section -->
            <section id="section-chats" class="tab-section active">
                <div class="chat-area">
                    <div class="chat-header d-flex justify-content-between align-items-center">
                        <div>
                            <h6 id="conversationTitle" class="mb-0">Select a conversation</h6>
                        </div>
                        <div class="d-flex gap-2">
                            <button id="takeOverBtn" class="btn btn-sm btn-outline-primary d-none">Take Over</button>
                            <button id="letBotHandleBtn" class="btn btn-sm btn-outline-success d-none">Let AI Respond</button>
                            <button id="deleteConversationBtn" class="btn btn-sm btn-outline-danger" title="Delete this conversation and all its messages"><i class="fas fa-trash"></i></button>
                        </div>
                    </div>
                    <div class="chat-row">
                        <div class="conversations-col">
                            <div class="conversations-header p-3 border-bottom bg-white">
                                <div class="input-group">
                                    <input type="text" id="searchInput" class="form-control" placeholder="Search conversations...">
                                    <button class="btn btn-outline-secondary" id="refreshBtn"><i class="fas fa-sync-alt"></i></button>
                                </div>
                            </div>
                            <div id="conversationsList" class="custom-scrollbar"></div>
                            <div class="load-more-container p-3 text-center">
                                <button id="loadMoreBtn" class="btn btn-outline-secondary w-100">Load More</button>
                            </div>
                        </div>
                        <div class="chat-messages-col flex-grow-1">
                            <div id="chatMessages" class="chat-messages"></div>
                            <div class="chat-input-area">
                                <div id="activeNotice" class="alert alert-info d-none mb-2">This conversation is being handled by the AI assistant. Take over to respond.</div>
                                <form id="messageForm">
                                    <input type="text" class="form-control" id="messageInput" placeholder="Type your message..." autocomplete="off">
                                    <button type="submit" class="send-btn" title="Send"><i class="fas fa-paper-plane"></i></button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            <!-- Team Section -->
            <section id="section-team" class="tab-section">
                <div class="container py-4">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h4 class="mb-0">Team Members</h4>
                        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addMemberModal">
                            <i class="fas fa-plus me-1"></i>Add Member
                        </button>
                    </div>
                    <div id="teamMembersList" class="row g-4"></div>
                </div>
                <!-- Add Member Modal -->
                <div class="modal fade" id="addMemberModal" tabindex="-1" aria-labelledby="addMemberModalLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="addMemberModalLabel">Add Team Member</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                            </div>
                            <div class="modal-body">
                                <form id="addMemberForm">
                                    <div class="mb-3">
                                        <label for="memberEmail" class="form-label">Member Email</label>
                                        <input type="email" class="form-control" id="memberEmail" required>
                                    </div>
                                    <div class="mb-3">
                                        <label for="memberRole" class="form-label">Role</label>
                                        <select class="form-select" id="memberRole" required>
                                            <option value="admin">Admin</option>
                                            <option value="member">Member</option>
                                        </select>
                                    </div>
                                </form>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                <button type="button" class="btn btn-primary" id="saveMemberBtn">Add Member</button>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            <!-- Widget Settings Section -->
            <section id="section-widget" class="tab-section scrollable">
                <div class="container py-4">
                    <div class="row g-4">
                        <div class="col-12 col-lg-6">
                            <div class="card shadow-sm">
                                <div class="card-body">
                                    <h4 class="mb-3">Widget Settings</h4>
                                    <form id="widgetSettingsForm">
                                        <div class="mb-3">
                                            <label class="form-label" for="widget_header_name">Header Name</label>
                                            <input type="text" class="form-control" id="widget_header_name" name="widget_header_name" value="<%= selectedBusiness.widget_header_name || selectedBusiness.name + ' Live Chat' %>" required>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label" for="widget_header_color">Header Color</label>
                                            <input type="color" class="form-control form-control-color" id="widget_header_color" name="widget_header_color" value="<%= selectedBusiness.widget_header_color || '#eee' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label" for="widget_h1_color">Header Title Color</label>
                                            <input type="color" class="form-control form-control-color" id="widget_h1_color" name="widget_h1_color" value="<%= selectedBusiness.widget_h1_color || '#000000' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label" for="widget_button_color">Send Button Color</label>
                                            <input type="color" class="form-control form-control-color" id="widget_button_color" name="widget_button_color" value="<%= selectedBusiness.widget_button_color || '#B31111' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label" for="widget_visitor_message_color">Visitor Message Color</label>
                                            <input type="color" class="form-control form-control-color" id="widget_visitor_message_color" name="widget_visitor_message_color" value="<%= selectedBusiness.widget_visitor_message_color || '#007bff' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label" for="widget_quick_replies">Quick Replies (one per line)</label>
                                            <textarea class="form-control" id="widget_quick_replies" name="widget_quick_replies" rows="4"><%= selectedBusiness.widget_quick_replies ? selectedBusiness.widget_quick_replies : '' %></textarea>
                                        </div>
                                        <button type="submit" class="btn btn-primary">Save Settings</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                        <div class="col-12 col-lg-6">
                            <div class="card shadow-sm">
                                <div class="card-body">
                                    <h4 class="mb-3">Live Preview</h4>
                                    <div class="widget-preview-container">
                                        <iframe class="widget-preview-iframe" id="widgetPreviewIframe" src="/widget/<%= selectedBusiness.id %>?preview=1" title="Widget Preview"></iframe>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            <!-- AI Settings Section -->
            <section id="section-ai" class="tab-section scrollable">
                <div class="container py-4">
                    <div class="row justify-content-center">
                        <div class="col-md-8">
                            <div class="card">
                                <div class="card-body">
                                    <h4 class="card-title mb-4">AI Agent Settings</h4>
                                    <form id="aiSettingsForm">
                                        <div class="mb-3" style="display: none;">
                                            <label class="form-label" for="chatbase_api_key">Chatbase API Key</label>
                                            <input type="password" class="form-control" id="chatbase_api_key" name="chatbase_api_key" value="<%= selectedBusiness.chatbase_api_key || '' %>" placeholder="Enter your Chatbase API key">
                                            <div class="form-text">Your Chatbase API key for AI agent integration</div>
                                        </div>
                                        <div class="mb-3" style="display: none;">
                                            <label class="form-label" for="chatbase_agent_id">Chatbase Agent ID</label>
                                            <input type="text" class="form-control" id="chatbase_agent_id" name="chatbase_agent_id" value="<%= selectedBusiness.chatbase_agent_id || '' %>" placeholder="Enter your Chatbase Agent ID">
                                            <div class="form-text">The ID of your Chatbase AI agent</div>
                                        </div>
                                        <hr class="my-4">
                                        <h5 class="mb-3" style="display: none;">N8N Integration</h5>
                                        <div class="mb-3" style="display: none;">
                                            <label class="form-label" for="n8n_webhook_url">N8N Webhook URL</label>
                                            <input type="url" class="form-control" id="n8n_webhook_url" name="n8n_webhook_url" value="<%= selectedBusiness.n8n_webhook_url || '' %>" placeholder="Enter your N8N webhook URL">
                                            <div class="form-text">The webhook URL for your N8N AI agent workflow</div>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label" for="n8n_system_prompt">System Prompt</label>
                                            <textarea class="form-control" id="n8n_system_prompt" name="n8n_system_prompt" rows="6" placeholder="Enter the system prompt for your AI agent"><%= selectedBusiness.n8n_system_prompt || '' %></textarea>
                                            <div class="form-text">The system prompt that defines your AI agent's behavior and personality</div>
                                        </div>
                                        <button type="submit" class="btn btn-primary">Save AI Settings</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            <!-- AI Training Info Section -->
            <section id="section-training" class="tab-section scrollable">
                <div class="container py-4">
                    <div class="row justify-content-center">
                        <div class="col-md-10">
                            <div class="card">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-center mb-4">
                                        <h4 class="card-title mb-0">AI Training Data</h4>
                                        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addTrainingModal">
                                            <i class="fas fa-plus me-1"></i>Add Training Data
                                        </button>
                                    </div>
                                    <div id="trainingDataList" class="table-responsive">
                                        <table class="table table-hover">
                                            <thead>
                                                <tr>
                                                    <th>Question</th>
                                                    <th>Answer</th>
                                                    <th>Last Updated</th>
                                                    <th>Actions</th>
                                                </tr>
                                            </thead>
                                            <tbody id="trainingDataTableBody">
                                                <!-- Training data will be loaded here -->
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Add Training Data Modal -->
                <div class="modal fade" id="addTrainingModal" tabindex="-1" aria-labelledby="addTrainingModalLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="addTrainingModalLabel">Add Training Data</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                            </div>
                            <div class="modal-body">
                                <form id="addTrainingForm">
                                    <div class="mb-3">
                                        <label for="trainingQuestion" class="form-label">Question</label>
                                        <textarea class="form-control" id="trainingQuestion" rows="3" required></textarea>
                                    </div>
                                    <div class="mb-3">
                                        <label for="trainingAnswer" class="form-label">Answer</label>
                                        <textarea class="form-control" id="trainingAnswer" rows="5" required></textarea>
                                    </div>
                                </form>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                <button type="button" class="btn btn-primary" id="saveTrainingBtn">Save Training Data</button>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            <!-- Install Widget Section -->
            <section id="section-install" class="tab-section scrollable">
                <div class="container py-4">
                    <div class="card shadow-sm mb-4">
                        <div class="card-body">
                            <h4 class="fw-semibold">Widget Embed Instructions</h4>
                            <p>Embed this chat widget on your website:</p>
                            <div class="mb-3 position-relative">
                                <textarea id="widgetEmbedCode" class="form-control" rows="12" readonly style="font-family: monospace; resize: vertical; min-height: 180px; background: #f8f9fa;">&lt;style&gt;
  #chat-launcher {
    position: fixed;
    bottom: 20px;
    right: 30px;
    z-index: 9999;
    width: 40px;
    height: 40px;
    cursor: pointer;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    background: <%= selectedBusiness.widget_button_color || '#007bff' %>;
    border-radius: 50%;
    box-shadow: 0 2px 8px rgba(0,0,0,0.10);
    transition: background 0.2s;
  }
  #chat-launcher svg {
    width: 20px;
    height: 20px;
    display: block;
    fill: #fff;
  }
  #chat-container {
    display: none;
    position: fixed;
    bottom: 70px;
    right: 30px;
    min-width: 350px;
    width: 30vw;
    height: 75vh;
    z-index: 9998;
    border-radius: 16px;
    overflow: visible;
  }
  #chat-iframe {
    width: 100%;
    height: 100%;
    border: none;
    border-radius: 16px;
    position: relative;
    z-index: 2;
  }
  #close-chat {
    position: fixed;
    bottom: 20px;
    right: 30px;
    width: 40px;
    height: 40px;
    background: #B31111;
    color: #fff;
    font-size: 28px;
    border: none;
    border-radius: 50%;
    cursor: pointer;
    z-index: 9999;
    display: none;
  }
&lt;/style&gt;

&lt;!-- 🔘 Chat Launcher Icon --&gt;
&lt;div id="chat-launcher" onclick="toggleChat()"&gt;
  &lt;svg viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg"&gt;
    &lt;path d="M476.59 3.41a32 32 0 0 0-32.91-7.19l-416 144A32 32 0 0 0 16 176.06c.7 13.19 10.19 24.44 23.06 27.19l153.25 33.5 33.5 153.25c2.75 12.87 14 22.36 27.19 23.06.53.03 1.06.04 1.59.04a32 32 0 0 0 31.06-23.91l144-416a32 32 0 0 0-7.06-32.78zM230.06 282.06l-28.12-128.62 128.62-28.12-100.5 156.74z"/&gt;
  &lt;/svg&gt;
&lt;/div&gt;

&lt;!-- 💬 Chat Container --&gt;
&lt;div id="chat-container"&gt;
  &lt;iframe
    id="chat-iframe"
    src="https://<%= host %>/widget/<%= selectedBusiness.id %>"
  &gt;&lt;/iframe&gt;
&lt;/div&gt;

&lt;!-- ❌ Rounded Close Button --&gt;
&lt;button id="close-chat" onclick="toggleChat()"&gt;×&lt;/button&gt;

&lt;script&gt;
  function toggleChat() {
    const container = document.getElementById('chat-container');
    const launcher = document.getElementById('chat-launcher');
    const closeBtn = document.getElementById('close-chat');
    const isOpen = container.style.display === 'block';
    container.style.display = isOpen ? 'none' : 'block';
    launcher.style.display = isOpen ? 'block' : 'none';
    closeBtn.style.display = isOpen ? 'none' : 'block';
  }
&lt;/script&gt;</textarea>
                                <button id="copyWidgetCodeBtn" class="btn btn-outline-primary position-absolute" style="top: 10px; right: 10px;">Copy Code</button>
                            </div>
                            <div id="copyWidgetCodeMsg" class="text-success ms-2" style="display:none;">Copied!</div>
                            <p class="mt-2">Or open your chat widget directly: <a href="https://<%= host %>/widget/<%= selectedBusiness.id %>" target="_blank">https://<%= host %>/widget/<%= selectedBusiness.id %></a></p>
                        </div>
                    </div>
                </div>
            </section>
        </div>
    </div>
    <audio id="notificationSound" src="/sounds/notification.mp3" preload="auto" style="display:none;"></audio>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/business_dashboard.js"></script>
    <script>
        // Sidebar tab navigation
        document.querySelectorAll('.sidebar-nav .nav-link').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                document.querySelectorAll('.sidebar-nav .nav-link').forEach(l => l.classList.remove('active'));
                this.classList.add('active');
                const tab = this.dataset.tab;
                document.querySelectorAll('.tab-section').forEach(sec => sec.classList.remove('active'));
                if (tab) document.getElementById('section-' + tab).classList.add('active');
            });
        });
        // Business selector change
        document.getElementById('businessSelector').addEventListener('change', function() {
            window.location.href = '/dashboard/' + this.value;
        });
        // Ensure logout link triggers a full page reload
        const logoutLink = document.querySelector('a[href="/logout"]');
        if (logoutLink) {
            logoutLink.addEventListener('click', function(e) {
                e.preventDefault();
                window.location = '/logout';
            });
        }
        document.getElementById('copyWidgetCodeBtn').addEventListener('click', function() {
            const textarea = document.getElementById('widgetEmbedCode');
            textarea.select();
            textarea.setSelectionRange(0, 99999); // For mobile
            document.execCommand('copy');
            const msg = document.getElementById('copyWidgetCodeMsg');
            msg.style.display = 'inline';
            setTimeout(() => { msg.style.display = 'none'; }, 1200);
        });
    </script>
</body>
</html> 